# PROJECT_MEMORY: evolve

## 1. Проект (одно предложение)
Система накапливания, верификации и применения знаний (скиллы, хурики, уроки, факты, предложения тулов) из успешно выполненных задач — чтобы кодинг-агент (DSH) со временем выполнял задачи успешнее и дешевле (ТЗ §1).

## 2. Цель и KPI
- Цель: success-rate задач выше baseline на ≥ 10 п.п. при токенах/задачу ≤ baseline (ТЗ §18, 3 месяца).
- KPI:
  - success-rate (по верификаторам) — целевое: +10 п.п. к baseline; текущее: baseline не измерен (нет телеметрии, M2)
  - ошибка-повтор категории — целевое: снижение ≥ 2×; текущее: нет данных
  - недельное окно аудитора — целевое: ≤ 20 мин; текущее: N/A (UI — M3)
  - база: ≤ 300 active (ТЗ A5)

## 3. Люди и стейкхолдеры
| Кто | Роль | Решает | Как/сколько часто общаться |
|---|---|---|---|
| разработчик (1 чел., ТЗ A1) | владелец, еженедельный аудитор (15–20 мин/нед + 1 ч/мес) | scope, принятие high-risk, пороги | еженедельное окно (M3), commit-одобрения |
| ИИ-агент | реализация по ТЗ | архитектура в рамках ТЗ, код, тесты | сессия; коммит — только с явного одобрения |
| DSH (агент-потребитель) | основной агент на старте (ТЗ A2) | — | через адаптер/M2, agent_id + профиль |

## 4. Решения (ADR-lite)
- **2026-09-14 — Стек: TypeScript ESM, Node 20+, единый бинарник (service + CLI).** Почему: ТЗ §5. Альтернативы: Go (отклонено — ТЗ прямо указывает TS), Bun/Deno (отклонено — Node как стандарт). Решение: ТЗ.
- **2026-09-14 — Хранилище M0: in-memory Store + DDL `db/schema.sql`; Postgres 16 + pgvector подключается с M2.** Почему: критерий M0 (ТЗ §15) не требует живой БД; PostgreSQL в рабочей среде отсутствует (проверено 2026-09-14); pgvector-индекс по ТЗ — задача M2. Альтернативы: установка PG16 сейчас (отклонено — избыточно для M0, решение пересмотреть при входе в M2). Решение: ИИ-агент.
- **2026-09-14 — Тесты: vitest, табличные тесты, инъекция зависимостей (время, LLM, БД) вместо моков глобальных.** Почему: awesome-coding `typescript/decisions.md` + `testing.md` (детерминированность, ТЗ §5). Решение: ИИ-агент.
- **2026-09-14 — Конфиг: один YAML (`config.yaml`) со всеми порогами/бюджетами, валидация zod на загрузке.** Почему: ТЗ §5 «все гейты параметризованы». Решение: ТЗ.

## 5. Ограничения и риски (top)
- Один разработчик, 15–20 мин/нед (ТЗ A1) — любой артефакт системы обязан влезать в недельное окно.
- Top-риск: галлюцинированные «уроки» в базе — митигация: G1 (верифицированный провенанс) + canary + demotion по score (ТЗ §16).
- Top-риск: качество LLM-экстрактора — митигация: метрика «ложных кандидатов» (M1), схемы+few-shot, причины отклонений → итерации (ТЗ §16).
- LLM-API (A4) в M0 не требуется (мок-LLM); потребуется с M1.

## 6. Текущее состояние (свежесть ≤ 1 недели)
- Фаза: M1 — экстрактор + гейты (по roadmap.md); M0 закрыт 2026-09-14 (4 коммита, 70/70 тестов)
- Готово: конвейер кандидата→гейты→статусы, CLI `evolve item ...`, DDL, конфиг, state-файл M0
- Следующее (M1): события телеметрии и триггер «успех задачи» → LLM-экстрактор → CLI-очередь high-risk
- Обновлено: 2026-09-14

## 7. Глоссарий
Термины ТЗ §6 (item, candidate, провенанс, risk-тир, canary, score, чирн, harness) — каноничное определение в `knowledge-evolution-tz.md` §6, здесь не дублируются.
